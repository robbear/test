
{
  "name": "test",
  "version": "1.0.0",
  "description": "Netlify redirect tests",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git://github.com/robbear/test"
  },
  "author": "robbear@hyperfine.com",
  "dependencies": {
  },
  "devDependencies": {},
  "engines": {
    "node": "10.16.0"
  },
  "scripts": {
    "build": "rm -rf ../build && mkdir -p ../build && cp -r ./ ../build/src"
  }
}